import { Text, View } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

export function MyLibrary({ childreen }) {
  return (
    <SafeAreaView>
      <View className='w-full h-[208px] '></View>
      <Text>Libraries22</Text>
    </SafeAreaView>
  )
}
