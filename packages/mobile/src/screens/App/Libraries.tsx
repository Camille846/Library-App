import { Text, View } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

export function Libraries({ childreen }) {
  return (
    <SafeAreaView>
      <Text>Libraries</Text>
    </SafeAreaView>
  )
}
